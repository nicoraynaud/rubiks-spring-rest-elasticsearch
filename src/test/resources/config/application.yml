spring:
    profiles:
        active: dev

rubiks:
    elasticsearch:
        cluster-nodes: http://localhost:9200
        context:
        test-mode: true
        username: int
        password: int
        scan-base-package: nc.rubiks
        indexed-objects:
        sync:
            enabled: true
            rate-milliseconds: 2000
            nb-retry: 3
